# 작업 계획서 템플릿 (설계 앞단 — 스펙 → 계획 → 실행)

> **규모 있는 작업(파일 3개 이상·새 기능·구조 변경)은 코드 전에 계획서 먼저.**
> 흐름: 브레인스토밍(질문으로 요구 좁히기) → 스펙 승인 → 이 계획서 → 실행 → 검증(§3).
> 사소한 수정(오타·한 줄 버그)엔 쓰지 않는다 — 과잉 프로세스도 실패다.
> 작성 위치: `docs/plans/YYYY-MM-DD-<주제>.md`. 다단계 장기 작업은 `master-plan-template.md`로 승격.

---

## 0. 스펙 (무엇을·왜 — 사용자와 합의된 것)

- **목적**: {{이 작업이 끝나면 사용자가 무엇을 할 수 있게 되는가 — 한 문장}}
- **범위 밖**: {{이번에 하지 않는 것 명시 — 범위 확장 방지}}
- **제약**: {{불변 결정(decisions-log)·기존 저장 호환·성능 등}}
- **성공 기준**: {{검증 가능한 형태로 — "○○ 테스트 통과", "△△ 화면에서 □□ 동작"}}

> 스펙이 모호하면 계획을 쓰지 말고 사용자에게 질문부터. 질문은 한 번에 하나씩, 선택지 제시형이 좋다.

## 1. 현황 조사 (계획 전 필수 — 추측 금지)

- {{관련 기존 구현 grep/정독 결과 — "이미 있는 것"을 다시 만들지 않기}}
- {{충돌 확인 — id·키·경로가 기존과 겹치는가}}
- {{참조 구현 — 따라할 모범 파일 경로}}

## 2. 접근 (2~3안 검토 후 선택)

| 안 | 요지 | 장점 | 단점 |
|---|---|---|---|
| A (채택) | | | |
| B | | | 기각 사유: |

## 3. 작업 분해 (실행 단위 — 각 단계는 독립 검증 가능하게)

| # | 작업 | 파일 경계 | 담당(메인/에이전트) | 검증 방법 | 완료 |
|---|---|---|---|---|---|
| 1 | | | | | ☐ |
| 2 | | | | | ☐ |

- 에이전트 위임 단계는 브리프 8요소(`agent-brief-template.md`) 발송.
- 병렬 가능 여부 = 파일 경계 겹침으로 판단. 공유 순차 파일 건드리는 단계는 순서 명시.

## 4. 완료 정의 (DoD — 전부 체크돼야 완료 선언)

- [ ] §3 전 단계 완료 + 각 검증 통과
- [ ] CLAUDE.md §3 전체 검증 루프 통과 (기준선 확인)
- [ ] 실제 구동 육안 확인 (테스트 통과 ≠ 동작)
- [ ] 문서 갱신 (CLAUDE.md §8 새 사실·결정 로그·이 계획서 완료 표시)

## 5. 리스크·롤백

- {{터질 수 있는 것 + 징후}} → {{대응}}
- 롤백: {{이 작업을 되돌리는 방법 — 커밋 단위·마이그레이션 역방향}}
