---
name: author-{{도메인}}
description: {{소스/명세}}에서 {{콘텐츠 종류}}를 저작하는 파이프라인 — "{{○○ 추가}}", "{{△△ 만들어}}" 요청 시 사용. (검증·감사 전용은 audit-{{도메인}}.)
---

# {{콘텐츠}} 저작 (author-{{도메인}})

{{입력(소스·명세·유형카드)}}을 받아 {{산출물}}을 **완성 세트**로 깎는다. 본체만 만들고 멈추면 미완이다.

> 짝 스킬: 감사 = `audit-{{도메인}}`. 불변식 전문 = 루트 `CLAUDE.md §2`. 브리프 정본 = `docs/agent-brief-template.md`.
> 에이전트 귀속: 1차 사용 = {{저작 에이전트}}. **최종 검증·커밋 = 오케스트레이터(메인).**

## 1. 완성 세트 (전부 갖춰야 커밋 가능)

| # | 산출물 | 어디에 | 규약 |
|---|---|---|---|
| ① | {{본체}} | {{경로}} | §3 |
| ② | {{설명/메타}} | {{경로}} | §4 |
| ③ | {{레지스트리 등록}} | {{경로 — 공유 순차 파일이면 명시}} | 순차 규칙 |
| ④ | 독립 검증 | 임시 테스트(제출 후 삭제) | §5-B |

## 2. 원본 취급 규칙 {{저작권 소스를 쓰는 경우 — 아니면 삭제}}

- 소스에서 가져오는 것은 **구조·개념뿐**. 표현·수치·소재는 복제도 미세변형도 금지 — 처음부터 새로 쓴다.
- **추적 테스트**: 산출물을 소스와 나란히 놨을 때 "같은 것의 변형"으로 식별되면 실패 → 더 비튼다.
- 소스 원본 폴더는 gitignore — 절대 커밋 금지.

## 3. 본체 규약

- {{불변식 전문 — CLAUDE.md §2에서 이 콘텐츠에 해당하는 것 복사}}
- {{등록·노출 방법 — 어느 배열/레지스트리에 append하면 자동 노출되는가}}
- {{금지 패턴 — 실버그 전력}}

## 4. 설명/메타 규약

- {{형식 정의 — 빌더 함수·단계 수 제한·통제 어휘}}
- 통제 어휘({{위치}})에 없는 토큰은 **PROPOSED 보고 → 메인 승인 → 등록** (직접 수정 금지).

## 5. 검증 (3중)

- **A. 자동**: {{속성 테스트 — 항목당 대량 샘플로 불변식 강제}}
- **B. 독립 재계산 (필수)**: 산출물의 **표시값에서** 정답/정합성을 다시 계산해 대조 — 구현 코드를 베끼지 말 것 (같은 로직 = 같은 버그). → `verifiers/independent-solver.md`
- **C. 육안 (생략 금지)**: 사용자가 실제 보는 텍스트를 임시 덤프로 직접 읽기 — 문법·의미·톤·{{도메인 적합성}}.
- **D. 자체 검증 루프 (에이전트가 커밋 전 자기 파일만 통과까지)**:
  ```
  {{타입체크}}
  {{자기 범위 테스트}}
  {{전체 실행 필수 검사 — "필터 금지" 명시}}
  ```

## 6. 합리화 방지 (이 유혹이 오면 멈춰라)

| 유혹 | 규율 |
|---|---|
| "필터로 내 것만 돌려 통과" | 교차 검사는 전체 실행만 정본. |
| "타 파일 위반도 같이 고치자" | 내 경계만. 타 파일은 보고. |
| "검증기 있으니 육안 생략" | C 생략 금지 — 검증기는 화면을 안 본다. |
| "새 토큰 필요하니 어휘에 바로 추가" | PROPOSED 보고 → 메인 승인. |
