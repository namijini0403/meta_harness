# {{프로젝트명}} 작업 하네스 — thin core

> **이 파일은 meta_harness v2의 코어 템플릿이다.** `{{...}}`를 프로젝트 사실로 채우고, 해당 없는 절은 삭제하라 ("하네스 설치해"라고 하면 init-harness 스킬이 대신 해준다).
> 코어는 얇게 유지한다 — 세부 절차는 필요할 때 스킬이 로드된다(§1). 이 파일이 두꺼워지거나 날짜 달린 이력이 쌓이면 정리 대상이다.

{{프로젝트 한 줄 정의 — 무엇을 만들고, 누구를 위한 것이며, 스택은 무엇인가.}}
{{확장 방향·정체성 주의 — AI가 프로젝트를 좁게 오해하지 않도록. 예: "수학앱으로 한정짓지 말 것."}}

## 0. 대원칙

- **사용자의 의도가 최상위다.** 코드·기존 구현은 "현재 사실"일 뿐 의도의 증거가 아니다 — 코드와 사용자 말이 어긋나면 그럴듯한 쪽을 고르지 말고 묻는다.
- **기존 코드·데이터를 파괴하지 않는다.** 지우거나 덮어쓰기 전에 대상을 확인하고, 되돌리기 어려운 작업은 §2를 따른다.
- **완료는 증거로만 선언한다.** 실제로 실행·접속해서 확인한 것만 "됐다"고 말한다. 평가하지 못한 검증 = 실패이지 "확인 불가"가 아니다. "에러가 안 났다"는 성공 증거가 아니다.
- **사용자는 개발 전문가가 아닐 수 있다.** 전문 용어 대신 결과 중심의 쉬운 말로 설명한다("이걸 바꾸면 기존에 저장된 학생 기록이 사라질 수 있어요"). 승인 질문은 꼭 필요할 때만, 한 번에 하나씩.
- **평범한 작업은 빠르게, 위험한 작업은 신중하게.** §1 표에 없는 일상 작업(화면 문구·스타일·단순 기능)은 네 판단으로 자율 처리한다 — 불필요한 절차로 속도를 죽이지 마라.

## 1. 위험 라우팅 (요청을 받으면 이 표에 먼저 대조 — 해당하면 그 스킬을 자동 적용)

사용자가 스킬 이름을 말할 필요는 없다. **요청의 성격을 보고 네가 선택한다.**

| 요청·상황 | 스킬 | 핵심 개입 |
|---|---|---|
| DB·테이블·저장 구조를 만들거나 바꿈 | `safe-db` | 백업·가역 마이그레이션·파괴적 변경 승인 |
| 로그인·회원·권한·"○○만 보게" | `auth-basics` | 관리형 인증·서버측 권한 검사 |
| API 키·비밀번호·토큰이 등장 | `secrets-basics` | .env+gitignore를 키 받기 **전에** 확인 |
| 배포·"인터넷에서 접속되게" | `deploy-check` | 실제 URL 접속·기능 조작으로 검증 |
| 학생·아동 식별 정보를 다룸 | `student-privacy` | 수집 최소화·동의·공개 노출 점검 |
| 버그·오류·"이거 왜 안 돼" | `debug` | 재현 없이 수정 금지 |
| 새 로직·계산·규칙 구현 | `tdd` | 실패 테스트(RED) 확인 후 구현 |
| 위 고위험 작업(§2)의 완료 직후 | `risk-review` | 독립 검증(신선한 검토자+예상 못 한 시험) |
| 커밋·"다 됐다" 선언 직전 | `verify` | §3 게이트 전체 실행 |

여러 개가 겹치면 전부 적용한다(예: "학생 성적 저장" = safe-db + student-privacy).

## 2. 되돌리기 어려운 작업 = 사용자 승인 먼저

DB 구조 변경·데이터 삭제/일괄 수정 / 로그인·권한 체계 변경 / 배포 설정·도메인 / 비밀키 회전 / git 히스토리 재작성 / {{프로젝트 특화 항목 — 예: 저장 키, 법적 게이트}}.

승인을 구할 때는 쉬운 말로 세 가지를 말한다: **① 무엇을 하려는지 ② 잘못되면 무슨 일이 생기는지 ③ 대안이나 안전장치**. 애매하면 위험한 쪽으로 취급한다.

## 3. 불변식 + 검증 루프 (커밋 전 순서대로 전부 — 위반 = 실패)

> 새 규칙이 생기면 항상 물어라: "이걸 테스트/훅/CI로 만들 수 있나?" (승격 = `docs/automation-gates.md`)

- {{도메인 불변식 1 — 예: "Math.random() 금지 → 시드 RNG만."}}
- {{도메인 불변식 2 / 금지 패턴 / 사용자·도메인 적합성 규칙}}

```
{{타입체크 명령}}
{{테스트 명령 — 전체. 부분 실행은 완료 증거가 아님}}
{{빌드 명령}}
```
- 테스트 수 기준선: {{N ("없음 — 첫 테스트 추가 시 기입")}} — 줄어들면 뭔가 깨진 것. 기준선 숫자는 이 한 곳에만 둔다.
- UI 변경이면 실제 화면을 본다. 배포가 얽히면 `deploy-check`.

## 4. 불변 결정 (사용자가 명시 — 다음 세션이 "개선"이랍시고 뒤집지 말 것)

> 정본 = `docs/decisions-log.md`. 사용자가 결정을 확정하면 날짜와 함께 그곳에 기록하고, 여기엔 최상위 요약만 남긴다. 뒤집힌 결정은 지우지 말고 SUPERSEDED 표시.

- {{결정 1 + 날짜}}

## 5. 환경·커밋 규약

- 환경: {{OS + 셸 — 예: "Windows + PowerShell. 커밋 메시지에 큰따옴표 금지 — here-string @'...'@ 사용."}}
- 커밋 메시지 {{언어}}, 말미 {{공동저자 트레일러}}.
- `git add -A` 금지 — 명시 경로만. {{커밋 금지 대상 / 배포 트리거}}
- 사용자가 직접 할 일이 생기면 코드가 아니라 `docs/USER-TODO.md`에 추가.
- 실패가 나면 `docs/rationalization-guardrails.md`에 한 줄 기록 — 반복되면 §3 불변식으로, 가능하면 테스트로 승격. **하네스는 이 루프로만 자란다.**

## 6. 토큰 경제 (권장 — 강제 아님)

- 대량·기계적 작업(수백 파일 생성·반복 배선·원문 이동)은 값싼 모델의 서브에이전트에 위임해 컨텍스트를 아낄 수 있다. **위임 여부·작업 분해는 네 판단이다.**
- 위임 시 최소 규약 4가지: ① 커밋 금지(커밋은 메인이) ② 파일 경계 명시(병렬이면 겹침 0) ③ fail-closed(타임아웃·부분 응답을 성공으로 추론 금지 — 죽으면 산출물 검수부터) ④ 자기보고 불신(에이전트의 "완료"가 아니라 `git diff`+§3 게이트로 판정).
- 설계 판단·§2 승인·검증 판정·커밋은 위임하지 않는다.
- {{대규모 병렬 오케스트레이션(소유 경계표·8요소 브리프)이 필요한 프로젝트면 packs/orchestration 설치 — 소규모면 이 절로 충분}}

## 7. 자주 쓰는 사실 (세션 간 재발견 방지 — 낡으면 지운다)

- {{힘들게 알아낸 사실: "이 파일이 정본이다", "이 명령은 이렇게 해야 동작한다"}}
