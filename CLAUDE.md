# {{프로젝트명}} 작업 하네스 (AI 전용 — 모든 모델이 동일 품질을 내기 위한 규약)

> **이 파일은 meta_harness 스타터킷의 코어 템플릿이다.**
> `{{...}}` 플레이스홀더를 프로젝트 사실로 채우고, 해당 없는 절은 삭제하라.
> 채우는 데 30분이면 충분하다 — 완벽하게 쓰려 하지 말고, 실패가 날 때마다 키워라(HARNESS.md §9).

{{프로젝트 한 줄 정의 — 무엇을 만들고, 누구를 위한 것이며, 스택은 무엇인가.
예: "초등 대상 드래곤 육성형 학습 PWA. 모노레포: client(React+Vite+TS) / server(Hono+pg)."}}

{{프로젝트의 확장 방향·정체성 주의사항 — AI가 프로젝트를 좁게 오해하지 않도록.
예: "수학앱으로 한정짓지 말 것 — 전과목·생활교육으로 확장 중."}}

## 0. 오케스트레이션 원칙 (토큰·품질 최적화)

- **메인 모델(너) = 계획·브리프 작성·검증·통합·커밋만.** 대량 구현은 서브에이전트에 위임.
- **검증 통과 판정·설계 결정·도메인 정확성 판단·커밋은 절대 위임하지 않는다.**
- 에이전트 위임 시 **`docs/agent-brief-template.md`의 8요소 브리프** 필수. 요약하지 말고 템플릿 복붙 + 델타만.
- 소유 경계·순차 파일·핸드오프 = `.claude/agents/README.md`가 정본. 서로 다른 파일만 만지면 병렬, 공유 파일은 순차.
- 에이전트가 중간에 죽으면: 남긴 파일을 `git status`로 확인 → "기존 산출물 검수부터"를 지시하는 이어받기 에이전트 재가동.
- 모델 배정: 설계·검수·승인 = 최상위 모델 / 대량 저작·배선 = 중간 모델 / 커밋·최종 판단 = 메인.
- {{장기 마스터플랜이 있으면: "⚠️ ○○ 작업은 `docs/{{마스터플랜}}.md` 필독·Phase 순서 강제 — 착수 전 읽고, 완료 시 상태판 갱신."}}

## 1. 반복 파이프라인 {{해당 없으면 삭제}}

{{프로젝트에서 반복되는 대량 작업(콘텐츠 추가, 모듈 추가, 마이그레이션 등)의 표준 절차를 번호 목록으로.
핵심 형식: ① 청사진 작성(메인) → ② 충돌 사전 확인(메인, grep) → ③ 구현 에이전트 N개 가동(경계 명시, 자체 검증 루프 포함) → ④ 통합 검증(메인, §3) → ⑤ 커밋.
예시는 packs/content-verification/pipeline-template.md 참조.}}

## 2. 불변식 (가능한 한 테스트로 강제 — 위반 = 실패)

> 새 규칙이 생기면 항상 물어라: "이걸 테스트로 만들 수 있나?" (HARNESS.md §4)

- {{도메인 불변식 1 — 예: "Math.random() 금지 → 시드 RNG만. 같은 seed = 같은 출력(재현성)."}}
- {{도메인 불변식 2 — 예: "부동소수점 직접 연산 금지 — 정수 스케일로 계산."}}
- {{금지 패턴 — 실버그 전력이 있는 코드 패턴. 예: "do{...continue...}while 금지 — 첫 반복 continue 시 미할당 탈출 실버그 전력."}}
- {{사용자·도메인 적합성 규칙 — 예: "학년 어휘 수준 준수. 폭력·잔인 소재 금지."}}

## 3. 검증 루프 (커밋 전 필수, 순서대로)

```
{{타입체크 명령    — 예: npx tsc --noEmit -p client && npx tsc --noEmit -p server}}
{{테스트 명령      — 예: npx vitest run  (전체 — 기존 테스트 포함 100% 통과)}}
{{빌드 명령        — 예: npm run build}}
{{추가 게이트      — 예: 중복 id 검사, 커버리지 프로브}}
```
- 부분 실행 방법: {{예: npx vitest run -t "<접두사>"}}
- **테스트 수 기준선: {{N}}** ({{날짜}}) — 줄어들면 뭔가 깨진 것. 테스트를 추가한 작업이 이 수치를 갱신한다.
- 상세 설계는 `docs/verification-loop.md`.

## 4. 외부 협업 체제 {{해당 없으면 삭제}}

{{AI가 직접 못 하는/안 하는 작업(이미지 생성, 디자인, 법률 검토 등)의 핸드오프 규약.
예: "이미지는 직접 생성하지 않는다. assets/SPEC*.md에 명세만 작성 → 담당(도구/사람)이 제작·커밋 → 앱은 폴백이 있어 부재가 블로커 아님."}}

## 5. 불변 결정 (사용자가 명시 — 되돌리지 말 것)

> 정본 = `docs/decisions-log.md` (템플릿: docs/decisions-log-template.md). 아래는 최상위 요약.

- {{결정 1 + 날짜 — 예: "저장 키 'xxx-save'는 변경 금지 (저장 호환)."}}
- {{결정 2 + 날짜}}

## 6. 데이터·개인정보 불변 결정 {{db-analytics/security-privacy 팩 미사용 시 삭제}}

- 이벤트는 화이트리스트({{위치}})만. **자유 텍스트·식별정보를 이벤트에 절대 넣지 않는다.**
- 새 이벤트 타입 = {{클라 타입 + 서버 허용 목록 + DDL 레지스트리}} **3곳 동기화**.
- 운영 DB와 분석 DB는 별도 — 코드에서 풀 혼용 금지.
- {{분석 DB URL}} 미설정 = no-op은 **법적 안전장치**(동의 전 수집 방지) — 우회 코드 금지.
- 상세 = `packs/db-analytics/` 및 `packs/security-privacy/`.

## 7. 환경·커밋 규약

- 환경: {{OS + 셸 — 예: "Windows + PowerShell. 커밋 메시지에 큰따옴표 금지(인자 깨짐) — 단따옴표 here-string @'...'@ 사용, 닫는 '@는 0열."}}
- 커밋 메시지 {{언어}}, 말미에 `Co-Authored-By: {{모델명}} <noreply@anthropic.com>` (실제 모델명으로).
- {{배포 트리거 — 예: "main 푸시 = 자동 배포 + CI 보안 게이트(비밀스캔·tsc·SAST)."}}
- {{커밋 금지 대상 — 예: "저작권 원본 폴더는 gitignore 유지, 절대 커밋 금지. git add -A 금지, 명시 경로만 add."}}
- 사용자가 직접 할 일이 생기면 코드가 아니라 `docs/USER-TODO.md`에 추가.
- 합리화 방지표(`docs/rationalization-guardrails.md`)에 실패를 기록하는 것이 하네스 유지보수의 전부다.

## 8. 자주 쓰는 사실 (세션 간 재발견 방지)

> 세션에서 힘들게 알아낸 사실("이 파일이 정본이다", "이 명령은 이렇게 해야 동작한다")을 여기 축적한다.
> 오래돼 틀려진 항목은 지운다. 이 절이 커지면 도메인별 docs로 분리.

- {{사실 1}}
- {{사실 2}}
